[project]
name = "models"
version = "0.1.0"
description = "Common models Lambda layer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
